# Development and testing dependencies for GHOSTLY+ EMG Analyzer

# Testing frameworks
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
pytest-clarity>=1.0.1 # Improves pytest output
httpx>=0.24.1  # Required for FastAPI TestClient

# Development tools - Modern Python tooling
ruff>=0.8.0          # Fast linter and formatter (replaces black, isort, flake8)
mypy>=1.13.0         # Type checking (latest version)

# Backend dependencies (also in pyproject.toml)
fastapi>=0.115.0
pydantic>=2.11.0
python-multipart>=0.0.9
numpy>=2.2.0
scipy>=1.15.0
ezc3d>=1.5.0 