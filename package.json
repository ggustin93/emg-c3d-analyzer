{
  "name": "emg-c3d-analyzer-root",
  "private": true,
  "scripts": {
    "build": "cd frontend && npm ci && npm run build && cd .. && rm -rf dist && mv frontend/dist .",
    "install": "cd frontend && npm ci"
  },
  "devDependencies": {
    "shadcn": "^3.3.0",
    "supabase": "^2.39.2"
  },
  "dependencies": {
    "@docusaurus/theme-mermaid": "^3.8.1"
  }
}
