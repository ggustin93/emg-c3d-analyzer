# Example Environment Variables for EMG C3D Analyzer Frontend
# Copy this file to .env for local development customization

# Backend API URL
# Default: http://localhost:8080 (matching start_dev.sh backend configuration)
# Production: Set to your deployed backend URL
VITE_BACKEND_URL=http://localhost:8080

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Storage Configuration
VITE_STORAGE_BUCKET_NAME=c3d-examples  # Must match backend STORAGE_BUCKET_NAME

# Demo credentials for testing (development only - DO NOT use in production)
VITE_DEMO_EMAIL=researcher@ghostly.be
VITE_DEMO_PASSWORD=ResearchGhostly2024!

# Note: In Vite, environment variables must be prefixed with VITE_
# to be accessible in the client-side code via import.meta.env