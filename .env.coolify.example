# EMG C3D Analyzer - Coolify Environment Configuration
# =====================================================
# Copy this file to .env in Coolify and configure your values

# Supabase Configuration (Required)
# ----------------------------------
# Option 1: Hosted Supabase (recommended)
# Get these from your Supabase project dashboard
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-supabase-anon-key
SUPABASE_SERVICE_KEY=your-supabase-service-role-key
SUPABASE_ANON_KEY=your-supabase-anon-key

# Option 2: Self-Hosted Supabase (advanced)
# For self-hosted Supabase on the same VM:
# SUPABASE_URL=http://supabase-kong:8000  # Internal Docker network
# SUPABASE_URL=https://supabase.your-domain.com  # External domain access
# SUPABASE_ANON_KEY=your-generated-anon-key
# SUPABASE_SERVICE_KEY=your-generated-service-key
# 
# Self-hosted additional variables (uncomment if using self-hosted):
# POSTGRES_PASSWORD=your-secure-postgres-password
# JWT_SECRET=your-40-character-jwt-secret
# SITE_URL=https://your-app-domain.com
# SMTP_HOST=your-smtp-server
# SMTP_PORT=587
# SMTP_USER=your-smtp-user
# SMTP_PASS=your-smtp-password

# Security Configuration (Required)
# ---------------------------------
# Generate a strong secret key for JWT tokens and sessions
SECRET_KEY=your-generated-secret-key-change-me
# Generate a webhook secret for Supabase Storage webhooks
WEBHOOK_SECRET=your-webhook-secret-change-me

# Application Configuration (Auto-configured by Coolify)
# ------------------------------------------------------
# Coolify automatically provides these variables
# COOLIFY_PUBLIC_URL=https://your-app.coolify-domain.com

# Redis Configuration (Internal - Do not change)
# ----------------------------------------------
REDIS_URL=redis://redis:6379/0
REDIS_CACHE_TTL_SECONDS=7200
REDIS_MAX_CACHE_SIZE_MB=200
REDIS_KEY_PREFIX=emg_analysis

# Production Settings (Recommended)
# ---------------------------------
ENVIRONMENT=production
LOG_LEVEL=INFO
DEBUG=false

# File Storage Configuration (Container paths)
# --------------------------------------------
DATA_BASE_DIR=/app/data
UPLOAD_DIR=/app/data/uploads
RESULTS_DIR=/app/data/results
CACHE_DIR=/app/data/cache

# Frontend Build Configuration (Auto-injected by Dockerfile)
# ----------------------------------------------------------
# These are automatically configured during Docker build:
# REACT_APP_API_URL=${COOLIFY_PUBLIC_URL}/api
# REACT_APP_SUPABASE_URL=${SUPABASE_URL}
# REACT_APP_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}