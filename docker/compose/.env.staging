# ═══════════════════════════════════════════════════════════════════════════
# EMG C3D Analyzer - Staging Environment Variables
# ═══════════════════════════════════════════════════════════════════════════
#
# Purpose: Environment-specific variables for staging deployment
# Usage: Referenced by docker-compose.staging.yml
# Deployment: Digital Ocean via Coolify Cloud
#
# ═══════════════════════════════════════════════════════════════════════════

# Environment Configuration
ENVIRONMENT=staging
VERSION=latest
DEBUG=false
LOG_LEVEL=INFO

# Service Configuration
ENABLE_REDIS=false  # Set to true if using Redis profile
ENABLE_METABASE=false
SUPABASE_MODE=cloud  # cloud or selfhosted

# Supabase Cloud Configuration
# Replace with your staging Supabase instance
SUPABASE_URL=https://your-staging-project.supabase.co
SUPABASE_ANON_KEY=your-staging-anon-key
SUPABASE_SERVICE_KEY=your-staging-service-key

# Frontend Configuration
# Replace with your staging domain
VITE_API_URL=https://staging-api.yourdomain.com
VITE_SUPABASE_URL=${SUPABASE_URL}
VITE_SUPABASE_ANON_KEY=${SUPABASE_ANON_KEY}
VITE_ENVIRONMENT=staging

# Backend Configuration
FRONTEND_URL=https://staging.yourdomain.com
ALLOWED_ORIGINS=${FRONTEND_URL}

# Security (Generate unique keys for staging)
SECRET_KEY=your-staging-secret-key-min-32-chars
WEBHOOK_SECRET=your-staging-webhook-secret

# Redis Configuration (if enabled)
REDIS_URL=redis://redis:6379/0
REDIS_CACHE_TTL_SECONDS=3600
REDIS_MAX_CACHE_SIZE_MB=100
REDIS_MAX_MEMORY=256mb
REDIS_KEY_PREFIX=emg_staging

# Optional: Metabase Analytics (if enabled)
METABASE_DB_USER=metabase
METABASE_DB_PASS=secure-staging-metabase-password

# Performance
BACKEND_WORKERS=2